import React from 'react';

// import { Container } from './styles';

type ProgressBarProps = {
  available: number;
  total: number;
};

const ProgressBar = ({ available, total }: ProgressBarProps) => {
  return <h1>Hello</h1>;
};

export default ProgressBar;
