import React from 'react';
import { Route, Redirect } from 'react-router-dom';

type Props = {
  isPrivate?: boolean;
}

const Router = (props: Props) => {
  const private = props.isPrivate;

  return (

  )
}
